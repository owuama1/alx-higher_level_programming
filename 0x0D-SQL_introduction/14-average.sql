/*
Script: Compute Score Average
Description: This script computes the score average of all records in the table second_table.
*/

-- SQL: Compute the average score
SELECT AVG(score) AS average
FROM `second_table`;
